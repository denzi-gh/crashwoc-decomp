# Crash WOC Agent Guide

Matching decompilation of Crash Bandicoot: The Wrath of Cortex for GameCube. The active retail target in this repo is `GCBE7D`, and the default local toolchain is `prodg35`. The compiler used is the ProDG 3.5 (with gcc 2.95.2 under the hood) and the compiler flags are written in configure.py

## Build and Verify

Run configure after changing `configure.py`, `config/GCBE7D/config.yml`, or `config/GCBE7D/splits.txt`:

```sh
python configure.py --version GCBE7D --toolchain prodg35
```

Core verification commands:

```sh
ninja
ninja progress
ninja baseline
ninja changes
```

For objdiff-driven regression checks in terminal automation, do not run interactive
`objdiff-cli diff` or other live-session commands. They stay open and refresh
continuously. Use one-shot report output instead:

```sh
ninja changes
python tools/changes_fmt.py build/GCBE7D/report_changes.json
```

If you need to call objdiff directly, use `objdiff-cli report changes ... -o <file>`
rather than the interactive diff UI.

Single-unit iteration:

```sh
ninja build/GCBE7D/src/<unit>.o
ninja build/GCBE7D/src/<unit>.ctx
```

## Generated Files

Do not hand-edit generated outputs:

- `config/GCBE7D/splits.generated.txt`
- `config/GCBE7D/config.generated.yml`
- `build.ninja`
- `objdiff.json`
- `compile_commands.json`

Edit the source-of-truth files instead, then rerun `python configure.py`.

## Repo Map

- [`config/GCBE7D/splits.txt`](config/GCBE7D/splits.txt): checked-in unit and section ownership
- [`config/GCBE7D/symbols.txt`](config/GCBE7D/symbols.txt): validated symbol names and addresses
- [`config/GCBE7D/config.yml`](config/GCBE7D/config.yml): dtk input config
- [`src/dump_alphaNGCport_DWARF.txt`](src/dump_alphaNGCport_DWARF.txt): large debug dump for fallback lookup
- `build/GCBE7D/report.json`: per-unit and per-function progress data after a build
- `build/GCBE7D/src/*.ctx`: decomp context targets generated by ninja

## Agent Tooling

Use the repo-local helpers before manual grep when possible:

- [`tools/ai_lookup_symbol.py`](tools/ai_lookup_symbol.py): lookup by symbol name or address, then cross-reference report data and owning split units
  - `python tools/ai_lookup_symbol.py FindAIType`
  - `python tools/ai_lookup_symbol.py 0x800032A0`
- [`tools/ai_lookup_unit.py`](tools/ai_lookup_unit.py): lookup by unit name, source path, build output, or address
  - `python tools/ai_lookup_unit.py ai.c`
  - `python tools/ai_lookup_unit.py src/system/gc/ai.c`
  - `python tools/ai_lookup_unit.py 0x800032A0`
- [`tools/ai_context.py`](tools/ai_context.py): one-stop context summary for a symbol or unit
  - `python tools/ai_context.py FindAIType`
  - `python tools/ai_context.py ai_1.c`
- [`tools/ai_decompme_zip.py`](tools/ai_decompme_zip.py): inspect a decomp.me-style zip or extracted directory, resolve target functions to repo units, and surface insertion/missing-declaration hints
  - `python tools/ai_decompme_zip.py C:/Users/denis/Downloads/MoveMINETUB.zip`
  - `python tools/ai_decompme_zip.py path/to/extracted_bundle_dir`

Existing helper scripts:

- [`tools/triage_build.py`](tools/triage_build.py): compile target triage
- [`tools/add_split_from_backup.py`](tools/add_split_from_backup.py): safe split import from `splits.elf_import_backup.txt`
- [`tools/generate_seed_splits.py`](tools/generate_seed_splits.py): seed `splits.txt` from map-style comments
- [`tools/extract_symbol_drafts.py`](tools/extract_symbol_drafts.py): draft symbols and DWARF candidates
- [`tools/decompctx.py`](tools/decompctx.py): raw decomp.me context generator
- [`tools/changes_fmt.py`](tools/changes_fmt.py): format `ninja changes` JSON output
- [`tools/validate_ai_tooling.py`](tools/validate_ai_tooling.py): validate repo-local agent docs and skills

## Skills

Repo-local skills live under `tools/skills/`:

- [`tools/skills/project-onboarding/SKILL.md`](tools/skills/project-onboarding/SKILL.md): use for first-pass repo discovery, build flow, and tool selection
- [`tools/skills/symbol-lookup/SKILL.md`](tools/skills/symbol-lookup/SKILL.md): use for symbol, address, unit, and DWARF lookup tasks
- [`tools/skills/split-maintenance/SKILL.md`](tools/skills/split-maintenance/SKILL.md): use when editing `splits.txt` or importing ranges safely
- [`tools/skills/build-triage/SKILL.md`](tools/skills/build-triage/SKILL.md): use for compile failures, progress checks, and regression review
- [`tools/skills/match-workflow/SKILL.md`](tools/skills/match-workflow/SKILL.md): use for symbol-to-verification matching workflows

## Workflow

For decomp work, prefer this order:

1. Resolve the target with `ai_lookup_symbol.py`, `ai_lookup_unit.py`, `ai_context.py`, or `ai_decompme_zip.py` for decomp.me-style bundles.
2. Build the single object or context target when iterating.
3. Keep changes narrow and match-focused.
4. Run `ninja changes` before wrapping up if matching or tooling behavior may have changed.

## Repo-Specific Notes

- `config/GCBE7D/splits.txt` is the checked-in split source; `configure.py` normalizes it for tools.
- `config/GCBE7D/symbols.txt` is the authoritative validated symbol list; use DWARF as a clue source, not as automatically trusted ground truth.
- `build/GCBE7D/report.json` is the easiest structured source for progress categories and per-function fuzzy match data.
- Prefer scripted split import and build triage over large manual edits when the workflow already exists in `tools/`.
- For automation, prefer `ninja changes` plus `python tools/changes_fmt.py build/GCBE7D/report_changes.json` over interactive `objdiff-cli diff` sessions.
- dont use `#pragma once` i nthe C Files

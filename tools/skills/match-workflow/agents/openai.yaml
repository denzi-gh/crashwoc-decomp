interface:
  display_name: "Match Workflow"
  short_description: "Guide symbol- or unit-based matching work"
  default_prompt: "Use $match-workflow to take a symbol or unit from lookup through planning, build, and verification."
